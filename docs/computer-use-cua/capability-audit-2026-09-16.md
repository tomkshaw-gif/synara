# Cua capability audit — 2026-09-16

This audit compares the pinned Cua Driver `0.28.2` surface with Synara's provider tools, backend, desktop host, and packaged runtime. The native tool surface is unchanged from the `0.24.0` audit; the rebase changed behavior inside the same tools (click delivery, cursor-overlay verification, snapshot identity) and is recorded in the [native patch notes](../../apps/desktop/patches/cua-driver/README.md). “Not agent-facing” is intentional where exposing a raw native operation would bypass Synara's exact-target, approval, cancellation, or observation contracts.

## Coverage

| Cua capability                                                                     | Synara use                                                                                       | Contract                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Desktop/window state, screen size, window listing, screenshots, Accessibility tree | `computer_get_state`, `computer_list_windows`, `computer_get_screen_size`, `computer_screenshot` | Read-only observations retain PID, window ID, geometry, Space membership, and screenshot freshness. Off-Space pixels are never presented as live grounding.                                                                                                                                                     |
| Click variants, cursor move, drag, scroll                                          | Matching `computer_*` actions                                                                    | Screenshot coordinates are converted through a registered frame and revalidated against the exact window. Pointer input remains active-Space-only.                                                                                                                                                              |
| Text, key press, hotkey                                                            | `computer_type_text`, `computer_press_key`, `computer_hotkey`                                    | Exact semantic text can use focus-neutral Accessibility insertion. Generic keyboard input remains active-Space-only and never falls back after uncertain delivery.                                                                                                                                              |
| Accessibility value/action                                                         | `computer_set_value`, `computer_perform_action`                                                  | Fresh label/role resolution is required. Ambiguous targets are refused. `perform_action` covers the driver's named AX actions (`press`, `open`, `show_menu`/`menu`, `pick`, `confirm`, `cancel`), gated on what the element advertises; semantic actions and value mutation never fall back to synthetic input. |
| Text selection (`select_text`, Synara patch rev 20)                                | `computer_select_text`                                                                           | Semantic element targets only — never coordinates. Verified only when a native `AXSelectedTextRange` read-back reports the exact range; unsettable attributes and marker-only web content refuse before dispatch with no click, key, or pointer fallback.                                                       |
| Clipboard                                                                          | `computer_read_clipboard`, `computer_write_clipboard`, `computer_paste`                          | Clipboard reads and writes are approval-gated. Paste restores the user's previous text and does not prove delivery from pixels alone.                                                                                                                                                                           |
| App launch and window activation                                                   | `computer_launch_app`, `computer_activate_window`                                                | Activation is foreground-only and approval-gated. Synara restores the previously frontmost window after the excursion.                                                                                                                                                                                          |
| App enumeration (`list_apps`)                                                      | `computer_list_apps`                                                                             | Read-only: reports running/active state, pid, bundle id, launch path, and last-used so an agent can tell "not running" from "not installed".                                                                                                                                                                    |
| Window frame (`set_window_frame`)                                                  | `computer_set_window_frame`                                                                      | Approval-gated, exact-window only, second-app consent applies. Verified only when an independent `list_windows` read-back shows the requested frame.                                                                                                                                                            |
| Menu invocation (`invoke_menu`)                                                    | `computer_invoke_menu`                                                                           | Approval-gated, exact-window only. Missing/ambiguous/disabled items refuse rather than fall back to pixels; effects are `unverifiable` unless the driver proves one.                                                                                                                                            |
| State verification (`verify_state`)                                                | `computer_verify_state`                                                                          | Read-only tri-state (`satisfied`/`unsatisfied`/`unknown`) with per-predicate evidence. Unknown never collapses into unsatisfied.                                                                                                                                                                                |
| Zoomed capture (`zoom`)                                                            | `computer_zoom`                                                                                  | Read-only magnified JPEG of a window-local rect. The result is display-only: it is never registered as a coordinate frame, so clicks cannot aim from it.                                                                                                                                                        |
| Process termination (`kill_app`)                                                   | `computer_kill_app`                                                                              | Approval-gated escalation after cooperative close fails. Resolves the exact window to its pid, and reports verified only when an independent read confirms exit.                                                                                                                                                |
| Input readiness and permissions                                                    | Desktop host provisioning and setup flow                                                         | Passive checks do not request grants. Missing Accessibility or Screen Recording raises one setup path; input fails closed.                                                                                                                                                                                      |
| Sessions and cancellation                                                          | `CuaDriverHost`, `DesktopOperationQueue`, `ComputerManager`                                      | Native sessions are internal lifecycle machinery. Turn cancellation closes admission, drains held input, and blocks unsafe replacement.                                                                                                                                                                         |
| Cursor configuration and recording state                                           | Ambient preview/runtime internals                                                                | Synara owns the user-facing preview and recording lifecycle instead of exposing competing native controls to providers.                                                                                                                                                                                         |
| Browser tools                                                                      | Separate `browser_*` surface                                                                     | Synara's integrated browser is controlled through structured browser tools, not desktop pixels. Computer Use remains the native-app and OS fallback.                                                                                                                                                            |

## Deliberately constrained native tools

| Native tool                                                                                                 | Decision                                                                                                                                                                                                                                            |
| ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Raw `start_session`, `end_session`, `escalate_session`, `get_session`, `list_sessions`, `get_session_state` | Internal only. Provider threads must not control native session ownership or outlive the gateway turn.                                                                                                                                              |
| Raw `start_recording`, `stop_recording`, `get_recording_state`, `replay_trajectory`, `install_ffmpeg`       | Internal only. Provider-controlled recording would conflict with Synara's preview privacy and lifecycle policy.                                                                                                                                     |
| `mouse_button_down`, `mouse_button_up`, `mouse_drag`, `parallel_mouse_drag`, `debug_window_info`            | Not registered on macOS — the names live in the shared contract vocabulary or other platforms' registries. `double_click`/`right_click` are dispatched as `click` `count`/`button` arguments instead.                                               |
| Raw `set_config`, `get_config`, `set_agent_cursor_*`                                                        | Internal only. Provider-controlled driver configuration and cursor ownership would bypass Synara's preview and consent model.                                                                                                                       |
| `check_permissions`, `check_input_ready`, `get_agent_cursor_state`                                          | Internal reads. The backend calls them for permission probes and pause recovery; no `computer_*` tool exposes them.                                                                                                                                 |
| `health_report`, `check_for_update`, `history_status`, `history_query`                                      | Internal diagnostics/lifecycle. Every `health_report` check has a Synara-native equivalent (permission probes, spawn handshake, socket liveness, capture health); upgrades belong to the app; history stays internal until a privacy policy exists. |
| `browser_*`, `page` family                                                                                  | Separate surface pending its own consent model. The desktop host does not allowlist them for Computer tools.                                                                                                                                        |

## 2026-09-17 update — rev-15 exposure

The `invoke_menu`, `set_window_frame`, and `verify_state` rows above were
originally "not agent-facing" pending exact ownership, approval semantics,
effect reporting, and platform tests. Those conditions are now met on the
pinned rev-15 driver: all three ship behind per-action approval, exact
PID/window revalidation at dispatch, and Synara-side read-back — a mutation
reports `verified` only when an independent observation confirms it, and an
uncertain dispatch is never replayed. `list_apps`, `zoom`, and `kill_app`
were verified live on TextEdit in the same pass and are exposed under the
same rules; `kill_app` is the destructive escalation of the set.

## Agent routing

Browser and Computer tools now refresh concise routing guidance on first use and every ten uses per thread. The counters are bounded and least-recently-used entries are evicted. Browser guidance prefers WebMCP, WebAgents, site requests, and structured DOM extraction before screenshots. It also defines a resumable bounded-batch strategy for long virtualized histories, including ordered text, links, images, GIFs, progress, deduplication, and explicit completeness limits.

Computer guidance requires observation before action, exact window IDs, semantic labels before coordinates, focus-neutral text only with a proven writable Accessibility target, foreground delivery only when activation is necessary, and no use of off-Space pixels as live evidence.

## Remaining certification boundary

The source and focused contract suites cover the routing, exact-target, concurrency, cancellation, Space, and freshness policies. Three simultaneous real-app targets with human foreground isolation are now verified at the semantic level on rev 17 (`evidence/rev17-realapp-3target-2026-09-22-*` — three distinct TextEdit pids, concurrent `set_value`, all `confirmed`, frontmost untouched, via external trusted host). Off-Space semantic input and the signed-app fresh-TCC run still require a freshly packaged revision-17 application with newly granted macOS permissions. The canonical Cua macOS harness additionally requires its documented SIP-disabled Lume maintainer environment and signing keychain; local smoke evidence is not a substitute.
