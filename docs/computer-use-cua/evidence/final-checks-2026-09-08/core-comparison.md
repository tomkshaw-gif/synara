# Final shared Computer core comparison

Compared the integrated worktree with the frozen refreshed PR822 (`bf70dfd0e7ee1e512a633da3cc6e814d2f791b0b`) and PR1010 (`32d62c28c39229a42f8d9f31b160bf61996e494a`) snapshots. Root reverified the remote heads. This pass reviewed actual implementation and focused coverage; it does not rely on the PR descriptions.

**Verdict:** the selected shared-core improvements are present, adapted to the Cua backend, while the local authority, exact screenshot targeting and no-replay protections remain. The audit found one concrete shared upstream/local pane-RPC cancellation defect; it has now been fixed with focused regression coverage. No additional required shared-core fix was found in the reviewed paths. This is not a claim that Cua implements every Swift-native capability or that whole-process RAM is lower under an equivalent native workload.

## Selected improvements present

Paths below are relative to `/Users/emanueledipietro/.codex/worktrees/702b/synara`; line anchors were refreshed after the current formatting pass.

| Area                         | Current evidence and integration decision                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Post-action perception       | `apps/server/src/computer/ComputerManager.ts:839` scopes newly opened-window fallback to the original process PID, with application-name fallback only when PID metadata is missing. Unrelated applications cannot redirect the returned observation.                                                                                                                                                                                      |
| Scroll targeting and overlap | `apps/server/src/agentGateway/computerTools.ts:1630` inherits the screenshot target, limits requested travel to half the captured region, and reports `limitedTo`; `ComputerManager.ts:1229` routes calibrated scrolling through the same pause/authority gate. Cua keeps its one-axis, single native dispatch and existing quantization instead of importing corrective input retries. Screenshot frame/window/bounds validation remains. |
| Conditional waiting          | `computerTools.ts:1132` adds post-action `wait_for_label`; `:1464` polls readiness with each read reacquiring manager authority and leaving the operation lane free between polls. A failed observation never repeats the completed action. Launch/window waiting is bounded and cancellation-aware.                                                                                                                                       |
| Pause and recovery           | `ComputerManager.ts:641,1749` exposes pause on read-only state and clears it only through the optional backend readiness predicate, with stale-state/removal guards. The Cua adapter implements actual exact-window native readiness; only a proven pre-dispatch off-Space refusal becomes pause. Uncertain dispatched effects remain distinct and non-retryable in `computerTools.ts:998`.                                                |
| State/activity cost          | `ComputerManager.ts:2383` publishes cached activity without fresh native reads, uses a global increasing version counter, and bounds idle records while preserving owner/in-flight/surfaced/paused records (`:2509`). The cap is intentionally soft for protected records. Cursor labels/debouncing and bounded optional contract fields are integrated.                                                                                   |
| Screenshot/scroll work       | `ComputerManager.ts:1485` preserves immutable measurement bytes; `scrollCalibration.ts:41,139,261` memoizes decode promises, yields during large row reconstruction, and rejects weak/aliased overlap estimates. `screenshotFrames.ts:62` memoizes source image hashes. These weak caches do not retain obsolete images by themselves.                                                                                                     |
| Publisher lifetime           | `stillFramePublisher.ts:104,122,138` uses attachment generations across prepare/capture/listener handoff and retains one in-flight capture. This closes the standalone stale-attach race; the manager already had epoch protection for ordinary pane detach, so this is not evidence of a previously proven production RAM leak.                                                                                                           |
| Diagnostic retention         | All selected canonical-ingress/logger/journal/Claude queue/snapshot stripping sites are present; local stripping also fixes cycle backreferences, sparse arrays, non-JSON objects, and the upstream overstrong generic return type. See the independent retention report for exact call sites and the preexisting bounded native-ingress limitation.                                                                                       |

## Cancellation defect found and fixed in this final pass

Before the fix, `wsComputerHandlers.attempt` discarded `Effect.tryPromise`'s AbortSignal, and the desktop queue did not inherit the caller's live signal. A human pane click/key queued behind another operation could dispatch after its RPC was interrupted. This weakness existed in the refreshed upstream path as well; it was not introduced by the selected imports.

The independent actual-source/FakeComputerBackend reproduction recorded zero inputs before interruption and **one input after queue drain**, for both pane key and click. Re-running the same script after the fix recorded **zero inputs after queue drain in both cases**. Evidence with source hashes: `/private/tmp/synara-socket-cancel-repro/result-before.json` and `/private/tmp/synara-socket-cancel-repro/result-after.json`.

The fixed `apps/server/src/computer/wsComputerHandlers.ts:63` binds the RPC signal through the desktop scope. `DesktopOperationQueue.ts:56,80` composes inherited live, explicit admission and shutdown signals, including explicit reentrant cancellation, while retaining the lane until the underlying native promise and cleanup settle. Root also added the expired-scope admission guard at `ComputerManager.ts:959`, so detached pane targeting cannot acquire a fresh transaction after its original call completes. Read-only/cosmetic work can still shed an expired turn signal, and `withoutDesktopCancellation` remains available for intentional release cleanup.

**23 focused tests passed** across `wsComputerHandlers.test.ts` and `DesktopOperationQueue.test.ts`: queued click/scroll/key interruption; active cancellation reaching backend work; cleanup retaining the lane; detached pane refusal; inherited and explicit cancellation composition; and nested-scope restoration. Log: `/private/tmp/synara-final-checks/pane-cancellation-tests.log`. These tests use a fake backend and send no OS input. Root owns the subsequent full verification pass.

## Safeguards retained and scope limits

- Manager input/activity entrypoints still reject expired contexts and revoked/suspended threads; generation revocation, active authority tracking, current-turn lease identity, shutdown/native-drain ordering, foreground approval and exact screenshot bounds are retained. An old completion cannot release a newer turn's lease. MCP arrival-time turn fencing and computer-off tombstones remain.
- Conditional reads remain serialized per native operation; no blanket 64-concurrent-capture/read lane was imported. Waiting releases the lane between polls without restoring stale input authority.
- Connection-owned event interests are removed on socket close and cannot be registered late. Provider image stripping leaves model-delivery objects intact. Existing bounded Codex native callback ingress and SDK-owned retention are separate from canonical diagnostic retention.
- Broader Swift-native capture, accessibility and input capabilities are adapter work, not a missing manager/schema flag. Native runtime qualification and equivalent-workload RSS measurements belong to the separate native/performance verification. Prior functional/helper tests were inspected; only the new handler/queue regression suite was rerun in this pass.

Independent supporting audit: `/private/tmp/synara-final-checks/retention-socket-audit.md` (its actionable section intentionally describes the reproduced pre-fix source).
